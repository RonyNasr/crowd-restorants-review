import { costHelper } from 'crowd-restaurants-review/helpers/cost-helper';
import { module, test } from 'qunit';

module('Unit | Helper | cost helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = costHelper([42]);
  assert.ok(result);
});
