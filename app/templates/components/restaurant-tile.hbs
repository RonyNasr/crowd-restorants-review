<div class="col-sm-6 col-md-4">
  <div class="thumbnail">
    <img src= {{restaurant.photo}}> <br>
    <div class="caption">
      <h3>{{restaurant.name}}</h3>
      <p>{{restaurant.cuisine}}</p>
      <p>${{restaurant.cost}}</p>
      <div class="add-review">
        <!-- {{#link-to 'map' restaurant.id}} <i class="fa fa-map-marker fa-2x" aria-hidden="true" {{action 'showMap' restaurant}}></i> {{/link-to}} &nbsp;&nbsp;&nbsp;
        {{#link-to 'shop.feedback' restaurant.id}} <button class="btn btn-info" >Feedback</button> {{/link-to}}-->
        <button class="btn btn-warning" {{action 'addReview' restaurant}}>Review</button>
      </div>
    </div>
  </div>
</div>
